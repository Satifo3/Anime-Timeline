# ANIME TIMELINE — v1.2

## ユーザー調整版アイコン反映
今回アップロードされた調整済みアイコン画像を、そのまま正式アイコン素材として採用しています。

反映先:
- icon-512.png
- icon-192.png
- apple-touch-icon.png
- favicon.png
- アプリ内ヘッダー表示

CSS側では余計な左右補正をせず、
`left:50%` / `object-fit:contain` で中央配置しています。

160作品、レコード機能、縦横UI、GitHub Pages用フラット構成はそのまま維持しています。
